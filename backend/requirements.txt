# --- Core (Phase 1) ---
Django==5.1.4
djangorestframework==3.15.2
django-cors-headers==4.6.0
psycopg2-binary==2.9.10
python-decouple==3.8
django-redis==5.4.0

# --- Phase 2: authentication, security & account foundation ---
djangorestframework-simplejwt==5.3.1   # JWT access/refresh tokens, delivered as HttpOnly cookies
argon2-cffi==23.1.0                    # stronger password hashing (PRD S4.4.1), set as PASSWORD_HASHERS[0]
google-auth==2.35.0                    # verifies Google ID tokens server-side for social login

# --- Banners/Promotions subsystem ---
Pillow==11.0.0                         # required by ImageField (banner uploads)

# --- Added in later phases, pinned now so builds stay reproducible ---
# celery==5.4.0                      # Phase 7: banner expiry, low-stock jobs
# channels==4.1.0                    # Phase 7: WebSocket stock/dashboard push
# channels-redis==4.2.0              # Phase 7
