import PagePlaceholder from "../../components/PagePlaceholder.jsx";

export default function AdminDashboard() {
  return (
    <PagePlaceholder
      title="Admin Dashboard"
      phase="Phase 6"
      description="KPI cards, recent orders, sales analytics — the platform control center (PRD §7/§6.1)."
    />
  );
}
