import PagePlaceholder from "../../components/PagePlaceholder.jsx";

export default function AdminLogin() {
  return (
    <PagePlaceholder
      title="Admin Login"
      phase="Phase 2"
      description="Hidden, unadvertised URL. Provisioned manually, not self-signup (PRD §4.1)."
    />
  );
}
