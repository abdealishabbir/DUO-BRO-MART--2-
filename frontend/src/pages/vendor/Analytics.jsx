import PagePlaceholder from "../../components/PagePlaceholder.jsx";

export default function VendorAnalytics() {
  return (
    <PagePlaceholder
      title="Analytics"
      phase="Phase 6+"
      description="Conversion rate, net order value, traffic sources and more — needs real order and store-view data, which land once the Order backend and storefront analytics exist."
    />
  );
}
