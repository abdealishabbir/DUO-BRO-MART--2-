import PagePlaceholder from "../../components/PagePlaceholder.jsx";

export default function VendorLogin() {
  return (
    <PagePlaceholder
      title="Vendor Login"
      phase="Phase 2"
      description="Hidden, unadvertised URL. Credentials issued by admin after approval; forces password change on first login (PRD §4.1/§5.1)."
    />
  );
}
