import PagePlaceholder from "../../components/PagePlaceholder.jsx";

export default function VendorOrders() {
  return (
    <PagePlaceholder
      title="Orders"
      phase="Phase 6+"
      description="Orders containing your products, with status and sales summary (§6.7) — needs the real Order backend, which lands once checkout moves off its current mock/localStorage system."
    />
  );
}
