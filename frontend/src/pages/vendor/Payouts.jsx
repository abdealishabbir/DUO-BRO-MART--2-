import PagePlaceholder from "../../components/PagePlaceholder.jsx";

export default function VendorPayouts() {
  return (
    <PagePlaceholder
      title="Payouts"
      phase="Phase 6+"
      description="Weekly payout breakdown and history (§6.7 notes this as its own ledger) — needs the real Order backend and a payout schedule/bank-transfer system, neither of which exist yet."
    />
  );
}
